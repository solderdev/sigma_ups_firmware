# Arduino board on Lattepanda Sigma

Handles UPS functionality of external UPS hat. 4 Li-Ion cells.

## Commands
compile with: make build

Uses the library at libs/DFRobot_LPUPS (git submodule) via the --library flag (not installed in the Arduino libraries folder).

Do not flash yourself! Flashing is done manually.

Do not change anything outside this directory.
